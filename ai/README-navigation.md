# AI navigation UI scope

This change keeps Assistente AI as the single entry point from the operational dashboard and adds a direct return path from every AI surface.

It does not change AI providers, prompts, persistence, Supabase schema, RAG behavior, Planner commit behavior, or fermentation contracts.
