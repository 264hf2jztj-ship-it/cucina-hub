const assert=require("node:assert/strict"),test=require("node:test"),core=require("./epub-reader-core.js");
const fakeFile=(name="Libro.epub",size=1024,lastModified=10)=>({name,size,lastModified});
test("accepts EPUB files up to 64 MB",()=>{assert.equal(core.validateFile(fakeFile()).name,"Libro.epub");assert.throws(()=>core.validateFile(fakeFile("Libro.pdf")),/EPUB valido/);assert.throws(()=>core.validateFile(fakeFile("Libro.epub",0)),/vuoto/);assert.throws(()=>core.validateFile(fakeFile("Libro.epub",core.MAX_EPUB_BYTES+1)),/64 MB/)});
test("builds a stable device-local progress key",()=>{const file=fakeFile("  Libro.EPUB  ",2048,99);assert.equal(core.bookKey(file),"libro.epub:2048:99");assert.match(core.progressStorageKey(core.bookKey(file)),/^cucina-hub:epub-progress:/)});
test("flattens nested table of contents preserving depth",()=>assert.deepEqual(core.normalizeToc([{label:"Parte 1",href:"p1.xhtml",subitems:[{label:"Capitolo",href:"c1.xhtml"}]},{label:"Senza link",subitems:[]}]),[{label:"Parte 1",href:"p1.xhtml",depth:0},{label:"Capitolo",href:"c1.xhtml",depth:1}]));
test("normalizes reader preferences and progress",()=>{assert.equal(core.clampFontSize(77),80);assert.equal(core.clampFontSize(114),115);assert.equal(core.clampFontSize(900),180);assert.equal(core.normalizeTheme("night"),"night");assert.equal(core.normalizeTheme("unknown"),"paper");assert.equal(core.progressPercent({start:{percentage:.426}}),43);assert.equal(core.progressPercent({}),null)});
